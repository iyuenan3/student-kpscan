# 依赖
node_modules/

# 构建产物
dist/
build/
src-tauri/target/
target/

# 环境 / 密钥（红线：绝不提交 key）
.env
.env.*
!.env.example
*.key
*.pem
secrets.*

# 测试数据 + spike 识别产物（含未成年人试卷及其转写文本，绝不入库）
samples/
test-data/
错题样本/
spike/out/

# Python
.venv/
venv/
__pycache__/

# AI 文档（私有，不公开；靠本地 + Claude memory 维护，跟其他项目惯例一致）
AIREADME/
CLAUDE.md

# 系统
.DS_Store
*.log
