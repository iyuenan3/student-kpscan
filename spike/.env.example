# newapi 网关（OpenAI 兼容）vision 端点配置。
# 复制本文件为 .env 后填写真实值。.env 已被 .gitignore 忽略，绝不提交。

# newapi 端点。填到 /v1 即可，脚本会自动补 /chat/completions。
# 也可直接填带 /chat/completions 的完整地址，脚本能识别。
NEWAPI_BASE_URL=https://你的newapi域名/v1

# newapi 令牌（sk- 开头）。建议用这种环境变量方式，不要写进命令行历史。
NEWAPI_KEY=sk-在这里填你的newapi令牌

# vision 模型名，按 newapi 里实际配好的渠道模型名填。
# 例：doubao-vision-pro-32k（豆包）/ qwen-vl-max（通义千问 VL）/ gpt-4o 等。
NEWAPI_MODEL=doubao-vision-pro-32k
